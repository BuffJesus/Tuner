// SPDX-License-Identifier: MIT
//
// HISTORY tab — about / quick-start / shortcut reference. No runtime
// state dependencies; built from theme tokens + the shared
// `make_info_card` factory.

#pragma once

class QWidget;

QWidget* build_history_tab();
