# Implementation Prompt

## Master Prompt

Use this to continue the project in a new session:

`Continue the Tuner project from docs/tuning-roadmap.md. Treat it as a Python PySide6 rewrite of TunerStudio for Speeduino-first workflows, not as a generic ECU settings app. The repo already contains: definition-driven INI/MSQ/project parsing (including #if/#else/#endif/#set/#unset preprocessor with active_settings override, lastOffset, arrayValue() in visibility expressions); DefinitionLayoutService and TuningPageService; a presenter-driven tuning workspace with clean/staged/invalid/written page state, sync state, RAM write, burn, revert, workspace review, operation log, quick-open, and command palette; scalar and table editing surfaces with undo/redo and staged table transforms; Engine Setup and Hardware Setup Wizard flows backed by shared operator context, setup summaries, validation, reqFuel helpers, and preset-assisted workflows; stage-only starter generators for VE, spark, AFR, idle RPM, WUE, cranking, and ASE; native Speeduino serial connect/read/write/burn/runtime support on the legacy raw command path; Runtime, Trigger Logs, and Flash surfaces; evidence/replay services including evidence history, pinned-vs-latest comparison, CSV datalog replay, replay review, trigger-log analysis, and page/table evidence hooks; VE Analyze and WUE Analyze both wired end-to-end; dashboard with DashboardPanel, DashboardLayoutService, per-gauge GaugeConfigDialog (INI-driven min/max/color zones from GaugeConfiguration), drag-and-drop rearrangement, DashTuning, and fullscreen. Logging is a first-class top-level tab (LoggingPanel): profile quick-switch dropdown with add/delete, Start/Stop/Clear/Save Log, configurable polling interval (250ms–5s), real-time capture-to-file, and Datalog Import & Replay. Service layer: DatalogProfile, DatalogProfileService (load_collection/save_collection multi-profile sidecar), LiveCaptureSessionService (start/stop/reset/append/CSV export/streaming output_path), DatalogImportService, DatalogReplayService, DatalogReviewService. Profile collection persisted as .logging-profile.json sidecar alongside the project file. Phase 9 board/firmware/runtime trust baseline is landed: FirmwareCapabilities.runtime_trust_summary() and uncertain_channel_groups(); BoardDetectionService.detect_from_capabilities() with capability-first precedence over text heuristics; SessionService.reconnect_signature_changed() and prior_firmware_signature; reconnect signature warning dialog + status bar message wired in MainWindow; uncertain_channel_groups() dimmed with tooltip in Runtime channel table. Curve editor complete: CurveDefinition/CurveYBins/CurveAxisRange models, _parse_curve_editors() INI pass, TuningPageKind.CURVE, CurvePageService wired into presenter load(); CurvePageSnapshot/CurveRowSnapshot in presenter; stage_curve_cell/undo_curve_param/redo_curve_param; CurveEditorWidget in tuning workspace (QTableWidget with read-only X column, editable Y columns, staged highlighting, undo/redo/write/burn buttons). INI gauge catalog complete: GaugeConfiguration domain model, _parse_gauge_configurations() (70+ named gauges with category, channel, title, units, lo/hi, warn/danger thresholds, digits); FrontPageIndicator domain model, _parse_front_page() (8 gauge slots + 40+ indicator expressions with on/off labels and colors); GaugeConfigDialog auto-fills title/units/min/max/color-zones from INI gauge catalog on source change. Production artifact round-trip tests complete: 31 tests covering speeduino 202501-T41 INI/MSQ/Ford300 in tests/fixtures/. TCP/WiFi transport complete: TcpTransport with Speeduino new-protocol framing (write_framed/read_framed_response: [u16 LE len][payload][u32 LE CRC32]), _send_data_command/_recv_data_response helpers on SpeeduinoControllerClient (framed for TCP, raw for serial — detected via getattr); Q/S handshake stays unframed (Airbear special-cases them); all data commands (r/p/M/b/f/t) now framed over TCP. Airbear v0.1.2 confirmed: port 2000, transparent proxy, mDNS speeduino.local, F/Q/S special-cased raw, all other commands expect new-protocol framing. boardCap_wifiTransport = byte 130 bit 7; runtimeStatusA = byte 147 bits 0-7 (fullSync=4, tuneLearnValid=7). HTTP Live-Data API complete: LiveDataHttpServer daemon-thread HTTPServer (port 8080), lock-protected snapshot, CORS headers, three endpoints. LoggerDefinition protocol complete: _parse_logger_definitions() INI pass; fetch_logger_data() on controller; LiveTriggerLoggerService decode + to_csv_path(); TriggerCaptureWorker QThread; live-capture UI in Trigger Logs tab. ControllerCommands dispatch complete: ControllerCommand domain model; _parse_controller_commands() INI pass (70+ commands); send_controller_command() on controller; HardwareTestPanel widget in Runtime tab. Current collected test count is 1595. Preserve the TunerStudio operating model: offline is first-class, INI metadata is the source of truth, tuning/runtime/logs/flash remain distinct operator surfaces, and all tune-changing assists remain staged and reviewable. Keep Qt widgets thin, keep business rules in services, add focused tests for every non-trivial service boundary, and prefer Speeduino-first workflow reconstruction over generic abstractions.`

## Current Phase Status

| Phase | Status |
|---|---|
| 0: Fixtures and compatibility baseline | **Complete** — U16P2 pair (19 tests) and production pair `speeduino 202501-T41` (31 tests) both covered; all fixtures in `tests/fixtures/` |
| 1: Definition fidelity and layout compilation | **Complete** — tables/scalars/dialogs/menus/`[CurveEditor]` (34 curves); `[GaugeConfigurations]` (70+ gauges with thresholds); `[FrontPage]` (8 slots + 40+ indicators) |
| 2: Editing surface parity | **Complete** — table, scalar, and 1D curve editing all done; `CurveEditorWidget` wired end-to-end |
| 3: Sync state and operation visibility | Complete for current baseline |
| 4: Hardware setup workflow surfaces | **Complete** — core setup surfaces, presets, reqFuel helpers, shared operator context, staged generators, and end-to-end wizard → staged → MSQ write integration test all landed |
| 5: Runtime evidence and replay | **Complete** — baseline + Phase 5 final polish (table cell density helper extracted, ceiling raised 56 → 80 px) |
| 6: Baseline VE Analyze compatibility | Complete; VE and WUE Analyze both wired end-to-end |
| 7: Better-than-TunerStudio assist | **Complete** — all seven slices landed; end-to-end pipeline test in place; workspace UI surfacing wired through `VeAnalyzeReviewService` (clamp count, boost penalty count, smoothed-layer summary, root-cause diagnostic lines added to `VeAnalyzeReviewSnapshot` with default-off backwards compat) |
| 8: Dashboard, workspace, and UX modernization | **Complete** — INI gauge catalog consumed; `GaugeConfigDialog` auto-fills from `GaugeConfiguration`; `FrontPage` parsed; LED indicator + static `label` widget kinds added (TSDash DashLabel parity) |
| 9: Board, firmware, and bench workflow integration | **Complete** — service layer, reconnect warning, uncertain channel dimming, TcpTransport + WiFi connect, LoggerDefinition live capture wired through `TriggerCaptureWorker` end-to-end test |
| 10: Hardening and release prep | Ongoing |
| 14: Native C++ Qt 6 desktop port | **In progress** — Slice 4 at sub-slice 84 (82 services + TableSurface3DView widget + formula output channels parser). C++ doctest suite: 1078 tests / 9699 assertions / 0 failures. Qt 6.7.3 built from source. `tuner_app.exe` has sidebar navigation with 8 pages, workspace presenter, animated gauges with sparkline histograms, live crosshair on table heatmaps, 2D↔3D toggle on table pages with rotatable wireframe surface and live operating-point crosshair (G2 closed), command palette (Ctrl+K), zone alerts, startup project picker (G1), humanized page names. All UX Phases A–D complete (19/19). Native format: `.tuner` + `.tunerproj` + `.tunerdef`. End-to-end pipeline tested against production fixtures. G4 (formula output channels) parser half landed — evaluator half deferred. |

## Recommended Next Prompt

`Continue Phase 14 (native C++ Qt 6 desktop port) from docs/tuning-roadmap.md. Current cursor: Slice 4 sub-slice 84. 82 services ported + TableSurface3DView widget + formula output channels parser (G4 Phase 1). C++ doctest suite: 1078 tests / 9699 assertions / 0 failures. Qt 6.7.3 built from source at C:/Qt/6.7.3-custom. All UX Phases A-D complete (19/19). The app has sidebar navigation with 8 pages: TUNE (search + humanized page names + editable scalars with smart warnings + table heatmaps with live crosshair + 2D↔3D toggle with rotatable wireframe surface + column-stretch grid + axis labels + project bar), LIVE (animated gauges + sparkline histograms + zone alerts + recording indicator + phase bar), FLASH (visual preflight checklist + firmware recommendation), SETUP (wizard step buttons + generators with confidence badges + sensor checklist + cross-validation), ASSIST (Tune Assist: VE + WUE analyze with plain-language summaries), TRIGGERS (visualization + analysis), LOGGING (datalog profile + replay), HISTORY. Startup project picker (G1). 3D table surface (G2) closed — TableSurface3DView with QPainter wireframe, mouse-drag rotation, 5-stop heat-colored edges, live operating-point crosshair via interpolate_screen_point. Command palette (Ctrl+K). Alt+1..8 + Ctrl+S shortcuts. Dynamic status bar. Native format: .tuner + .tunerproj + .tunerdef. Workspace presenter + state machine. Known issue: table heatmap cell sizing on splitter resize needs further work. Natural next: (1) real ECU connection wiring, (2) table cell sizing improvements, (3) formula channels (G4). Runtime DLLs: copy libgcc_s_seh-1.dll + libstdc++-6.dll + libwinpthread-1.dll from MinGW bin to build dir alongside Qt DLLs. Build: cmake -S cpp -B build/cpp -G "MinGW Makefiles" -DCMAKE_BUILD_TYPE=Release -DCMAKE_PREFIX_PATH=C:/Qt/6.7.3-custom -DTUNER_BUILD_APP=ON -DTUNER_CORE_BUILD_TESTS=ON; cmake --build build/cpp --config Release -j 4. Run tests: cd build/cpp && cmd //c ".\tuner_core_tests.exe".`

## Previous Recommended Prompt (Python phases — now superseded by Phase 14)

`Continue from docs/tuning-roadmap.md. Current test count: 1805. Phases 0–6 and 8–9 complete; Phase 4 closed by `test_hardware_setup_wizard_msq_round_trip.py` (wizard → generator → MsqWriteService round-trip on production INI/MSQ); Phase 9 closed by `test_trigger_capture_worker.py` (end-to-end TriggerCaptureWorker fake-client + failure path). Next phase to attack is Phase 7 — see "Phase 7 Scope" in docs/tuning-roadmap.md. The first Phase 7 slice is firmware-gated sample acceptance: wire `runtimeStatusA` `tuneLearnValid` (and `transientActive` / `warmupOrASEActive` negatives) into `ReplaySampleGateService` / `AutotuneFilterGateEvaluator` as a hard gate, opt-in via a new `FirmwareLearnGateConfig`, with focused service tests.`

## Secondary Prompt: Curve Editor

`Continue from docs/tuning-roadmap.md and implement the curve editor slice. The production INI has 34 curve definitions in [CurveEditor]. Each curve has: a name, display title, xBins (parameter name + live output channel), yBins (parameter name + live output channel), columnLabels, and xAxis/yAxis range/step hints. Add a CurveDefinition domain model to ecu_definition.py, a [CurveEditor] parser pass in ini_parser.py that produces list[CurveDefinition] on EcuDefinition, a CurvePageService that compiles curves into editor-facing pages (analogous to TuningPageService for tables), and a CurveEditorWidget that renders the curve as an editable row-based table (x-axis constants in one column, editable y-values in another). Wire the curve pages into the tuning workspace navigator alongside table pages. Keep the edit path staged (same LocalTuneEditService.stage_list_cell path as table cells). Add focused tests for the parser pass and service.`

## Secondary Prompt: Production Artifact Round-Trip Tests

`Continue from docs/tuning-roadmap.md and add production artifact round-trip tests. The release folder at C:/Users/Cornelio/Desktop/speeduino-202501.6/release/ has: speeduino-dropbear-v2.0.1.ini (signature speeduino 202501-T41, standard byte-serialized contract) and speeduino-dropbear-v2.0.1-base-tune.msq (15 pages, same signature). Copy both to tests/fixtures/ and write test_production_round_trip.py mirroring test_release_round_trip.py: fixture sanity, scalar edit round-trips (reqFuel, nCylinders), table cell edit round-trips (veTable), preservation tests for any lastOffset-derived tables, output format sanity. Also check whether Ford300_TwinGT28_BaseStartup.msq (non-U16P2) is compatible with the production INI and add fixture tests for it if so.`

## Secondary Prompt: Bench / Runtime Trust UI Wiring

`Continue from docs/tuning-roadmap.md and wire the Phase 9 trust helpers into the MainWindow UI. Two concrete tasks: (1) In MainWindow._on_connect() (or wherever connect success is handled), call session_service.reconnect_signature_changed() and if True show a visible warning — a status bar message and/or a non-blocking info dialog — that tells the operator the firmware signature changed since the last session, and they should re-validate their tune/definition; (2) In the Runtime channel table display, call firmware_capabilities.uncertain_channel_groups() on the current session capabilities and add a visual flag (e.g. color, tooltip, or suffix) to channel rows whose group name matches an uncertain group. Both changes should be thin UI wiring on top of already-tested service logic — no new service tests needed unless the wiring introduces non-trivial logic.`

## Secondary Prompt: HTTP Live-Data API (Our TSDash Phase 1)

`Continue from docs/tuning-roadmap.md. Implement Phase 1 of our TSDash replacement: an opt-in HTTP live-data server. When enabled (preference or toolbar toggle), start a background HTTP server (default port 8080) that serves the latest OutputChannelSnapshot as JSON. Endpoints: GET /api/channels → {channel: value, ...}; GET /api/channels/{name} → single value; GET /api/status → connection/sync state. Wire to the runtime poll tick — same snapshot the DashboardPanel already uses. The server must not block the Qt event loop (run in a daemon thread). This makes our app a drop-in data source for any browser, Raspberry Pi, or phone on the local network — no TunerStudio relay needed.`

## Secondary Prompt: TCP Transport + WiFi Connection (Our TSDash Phase 3)

`Continue from docs/tuning-roadmap.md. Add TcpTransport(host, port=2000) that speaks identical Speeduino framing to the existing SerialTransport — no protocol changes, just a socket instead of a COM port. The Airbear firmware (C:\Users\Cornelio\Desktop\Airbear-main, v0.1.2) already implements the TunerStudio TCP bridge on port 2000 and is the official ESP32-C3 firmware for the DropBear. It handles the 'F'/'Q'/'S' identification handshake and the length-prefixed new protocol transparently. Wire a "Connect via WiFi" option into the connection dialog, shown and enabled only when boardCap_wifiTransport is set. Also support mDNS resolution of speeduino.local (default hostname). The DropBear ESP32-C3 SUPERMINI connects to Teensy Serial2 (pins 7/8, MCU-D7/MCU-D8) at 115200 baud — confirmed from v2.0.1 schematics and Airbear source.`

## Completed Slices

- `tests/unit/test_release_round_trip.py` (19 tests) — U16P2 experimental pair (`Ford300_TwinGT28_BaseStartup_u16p2_experimental.msq`, `speeduino-dropbear-v2.0.1-u16p2-experimental.ini`) in `tests/fixtures/`
- Logging tab complete — `LoggingPanel`, multi-profile sidecar, real-time capture, profile-ordered review
- Phase 9 service baseline — `detect_from_capabilities()`, `reconnect_signature_changed()`, `FirmwareCapabilities` trust helpers, 29 new tests
- Curve editor full slice — `CurveDefinition`/`CurveYBins`/`CurveAxisRange` domain models; `_parse_curve_editors()` INI pass (38 tests); `TuningPageKind.CURVE` + curve fields on `TuningPage`; `CurvePageService` with keyword group classification (20 tests); bug fixes: bin params are `TableDefinition` not scalars; `"ve"` keyword word-boundary match; 1362 tests total
- TSDash decompilation — 4946 files from `TSDash.jar` via jd-gui batch decompiler; 17 named gauge painter types identified; architectural limitations catalogued; full comparison table vs our implementation in `docs/tuning-roadmap.md`
- DropBear schematic review — ESP32-C3 SUPERMINI confirmed on Teensy **Serial2** (pins 7/8, MCU-D7/MCU-D8); plug-in module, own USB-C; BLE + WiFi confirmed; Serial1 (pins 0/1) is the user-exposed secondary serial header (separate); all sensor conditioning, output drivers, stepper, power rails documented in `docs/tuning-roadmap.md`
- Airbear ESP32 firmware review (`C:\Users\Cornelio\Desktop\Airbear-main` v0.1.2) — three modes: Dash (30Hz web JSON + SSE), BLE UART (NimBLE NUS), TunerStudio TCP bridge on **port 2000**; Dash mode channel list catalogued; mDNS at `speeduino.local`; TCP transport confirmed port 2000
- Phase 0 complete: production artifact round-trip tests — 31 tests covering `speeduino-dropbear-v2.0.1.ini` + `speeduino-dropbear-v2.0.1-base-tune.msq` + `Ford300_TwinGT28_BaseStartup.msq` (all in `tests/fixtures/`); 1393 tests total
- Comprehensive roadmap update — firmware roadmap cross-reference (Phases 1-10, `LOG_ENTRY_SIZE=148`, `runtimeStatusA` bits, capability facts); JSON5/JSON native contract deliverables expanded; `TcpTransport` priority added; post-roadmap program updated with concrete scopes and firmware-gated autotune gating strategy
- CurveEditorWidget complete — `CurvePageSnapshot`/`CurveRowSnapshot` presenter dataclasses; `CurvePageService` wired into presenter `load()`; `stage_curve_cell`/`undo_curve_param`/`redo_curve_param` public methods; `CurveEditorWidget` QTableWidget (read-only X column, editable Y columns, staged highlighting, undo/redo buttons, revert/write/burn wired); 32 focused tests; 1425 tests total
- INI gauge catalog + FrontPage complete — `GaugeConfiguration` domain model; `_parse_gauge_configurations()` (70+ gauges with category, channel, title, units, lo/hi, warn/danger, digits); `FrontPageIndicator` domain model; `_parse_front_page()` (8 gauge slots + 40+ indicator expressions); `GaugeConfigDialog` wired to `set_gauge_configurations()` — source change auto-fills title/units/min/max/color-zones from INI; 36 focused parser tests; 1461 tests total
- TCP transport + WiFi connect complete — `TcpTransport` improved: `clear_buffers()`, `sendall()`, per-call read timeout, mDNS `.local` extended timeout; "Connect via WiFi" button in Runtime panel prefills `speeduino.local:2000`/TCP/SPEEDUINO and connects; button shows `✓` when `boardCap_wifiTransport` telemetry bit is set; 11 loopback socket tests; 1472 tests total
- Speeduino new-protocol TCP framing — `TcpTransport.write_framed()` + `read_framed_response()`: `[u16 LE len][payload][u32 LE CRC32]`; `SpeeduinoControllerClient._send_data_command()` + `_recv_data_response()` detect framing capability via `getattr`; all data paths (runtime read, page read/write, burn, calibration, capabilities 'f') now framed over TCP, unframed over serial; Q/S handshake stays raw (Airbear F/Q/S special-case path); 7 framing unit tests + 12 controller framing tests; 1491 tests total
- HTTP Live-Data API complete — `LiveDataHttpServer` daemon-thread `HTTPServer` (port 8080); `threading.Lock`-protected snapshot + status; CORS on all responses; `GET /api/channels` → flat channel dict; `GET /api/channels/{name}` → single channel with units; `GET /api/status` → connected/sync_state/port; wired to runtime poll tick via `update_snapshot()`; `update_status()` called on connect and disconnect in `MainWindow`; "Start/Stop Live Data API" button in Runtime panel; 20 focused tests; 1511 tests total
- LoggerDefinition protocol complete — `LoggerRecordField`/`LoggerDefinition` domain models; `_parse_logger_definitions()` INI pass (4 loggers: tooth H/h, composite J/j/O/o/X/x; `\$tsCanId` substitution + `\xNN` decode; `calcField` skipped); `SpeeduinoControllerClient.fetch_logger_data()` (start → poll `toothLog1Ready` bit → `dataReadCommand` → read raw → stop); `LiveTriggerLoggerService.decode()` (bit-level extraction for 1-bit flags and u32 LE fields; scale applied); `TriggerLogCapture.to_csv_path()` for analysis pipeline hand-off; `TriggerCaptureWorker` QThread; live-capture section in Trigger Logs tab (logger combo + Capture Live Log button, disabled offline); 55 focused tests; 1566 tests total
- ControllerCommands dispatch complete — `ControllerCommand` domain model; `_parse_controller_commands()` INI pass (70+ commands: test mode, 8× injector on/off/pulsed, 8× spark on/off/pulsed, STM32 reboot/bootloader, SD format, VSS calibration; `\xNN` decode); `SpeeduinoControllerClient.send_controller_command(payload)`; `HardwareTestPanel` widget (test mode, injector/spark selector combos + on/off/pulsed buttons, utilities section; enabled only when connected); wired into Runtime tab with connect/disconnect/definition-load hooks; 29 focused tests; 1595 tests total
- Phase 4 close-out — `test_hardware_setup_wizard_msq_round_trip.py` drives the production INI/MSQ pair through `HardwareSetupWizard._on_generate_ve_table()` → `MsqWriteService.save()` → `MsqParser.parse()` and asserts the wizard-staged VE table values land in the saved MSQ exactly; closes the "wizard input → staged → real MSQ write" gap in the subsystem matrix; 1617 tests total
- Phase 9 close-out — `test_trigger_capture_worker.py` exercises `TriggerCaptureWorker` end-to-end against a fake `SpeeduinoControllerClient` (success path: synthetic u32 LE tooth bytes decode through real `LiveTriggerLoggerService` and round-trip the analysis-pipeline CSV; failure path: client raises → `failed` signal); promotes the LoggerDefinition row in the subsystem matrix from Partial → Implemented; 1619 tests total
- Phase 7 Slice 7.1 (firmware-gated sample acceptance) — `firmware_learn_gate_enabled` flag on `SampleGatingConfig` plus a new `firmwareLearnGate` in `ReplaySampleGateService` that hard-rejects samples unless `runtimeStatusA` shows `fullSync && tuneLearnValid && !transientActive && !warmupOrASEActive` (bits `0x10/0x80/0x20/0x40`); falls back to accept when the channel is absent so legacy logs and offline replay are unchanged; default-off behaviour is bit-identical to Phase 6 baseline; runs first when enabled so the operator's primary rejection reason is the strongest signal; 12 focused tests in `test_firmware_learn_gate.py`; 1631 tests total
- Phase 7 Slice 7.2 (per-cell weighted correction with bounded edits) — new `WeightedCorrectionConfig` (opt-in, default-off) with `max_correction_per_cell`, `dwell_weight_enabled`, `dwell_weight_cap_seconds`, `sample_age_decay_per_second`; `VeAnalyzeCellHitAccumulator` storage moved to `list[(correction, weight, timestamp)]`, weighted mean reduces to arithmetic mean when all weights are 1.0 (Phase 6 baseline preserved bit-identically); new `raw_correction_factor` + `clamp_applied` transparency fields on `VeAnalysisProposal` and `VeAnalysisCellCorrection`; dwell weight = `1 + min(time_delta, cap)` against the previous accepted sample in the same cell; sample age decay applied at snapshot time as `weight *= exp(-age * decay)` against the latest accepted timestamp; 9 focused tests in `test_ve_analyze_weighted_correction.py`; 1640 tests total
- Phase 7 Slice 7.3 (confidence and coverage reporting) — continuous `confidence_score = 1 - exp(-n/10)` on `VeAnalysisCellCorrection` (n=10 → 0.63, n=30 → 0.95, monotonic, saturates at 1.0); new `CoverageCell(row, col, sample_count, confidence_score, status)` and `VeAnalysisCoverage(rows, columns, cells, visited_count, total_count, coverage_ratio)` built from the table snapshot dimensions so unvisited cells are explicit; new `coverage` field on `VeAnalysisSummary` (default `None` so legacy hand-built fixtures stay valid); `VeAnalyzeReviewService` detail text includes `Coverage: X/Y cells (N%) visited.` when coverage is present; 10 focused tests in `test_ve_analyze_confidence_coverage.py`; 1650 tests total
- Phase 7 Slice 7.4 (steady-state refinements with EGO transport-delay compensation) — new `SteadyStateConfig(ego_transport_delay_seconds, max_drpm_per_second, max_dmap_per_second, history_window_seconds)` (opt-in, default-off); `VeAnalyzeCellHitAccumulator` keeps a rolling `_history` of recent records trimmed to `history_window_seconds` on each `add_record` call; when `delay > 0`, the lambda/AFR/EGO channels of each incoming record are merged onto the engine state from the history entry closest to `t - delay` and the synthesized record is fed through the existing cell-mapping path (samples that arrive before the history covers the delay window are rejected as `delay_buffer_cold`); derivative gates compute `|drpm/dt|` and `|dmap/dt|` against the most recent prior record and reject as `transient_rpm_derivative` or `transient_map_derivative` when above threshold; defaults preserve the Phase 6 baseline bit-identically; 9 focused tests in `test_ve_analyze_steady_state.py`; 1659 tests total
- Phase 7 Slice 7.5 (smoothing as a reviewable transform) — new `VeProposalSmoothingService` + `SmoothingConfig(kernel_radius, min_neighbors, preserve_edge_magnitude)` + `SmoothedProposalLayer` in `ve_proposal_smoothing_service.py`; stateless service reads `VeAnalysisSummary.proposals` without mutating them and produces a *separate* smoothed proposal layer so the operator can stage raw, smoothed, or neither; smoothing only touches cells that already received a raw proposal (never invents VE values for unvisited cells); edge cells shrink the kernel to existing neighbors instead of fabricating values; sample-count-weighted average prevents a low-n neighbor from pulling a high-n anchor; `min_neighbors` requires N neighbors before smoothing fires; `preserve_edge_magnitude` keeps the strongest correction in the kernel intact (boost-spool transitions stay sharp); `kernel_radius=0` is a documented identity transform; smoothed proposals carry the original cf in `raw_correction_factor` for review transparency; 9 focused tests in `test_ve_proposal_smoothing_service.py`; 1668 tests total
- Phase 7 Slice 7.6 (boost-aware confidence penalties) — new `BoostConfidenceConfig(atmospheric_kpa, spool_drpm_threshold, spool_dmap_threshold, spool_penalty_max, mat_dt_threshold, mat_penalty_max)` + closed-form `_boost_confidence_multiplier()` returning a multiplier in `[1 - max_pen, 1.0]`; spool transition penalty active *only* in positive boost (`MAP > atmospheric_kpa`), MAT instability penalty active in vacuum and boost; severities are linear ramps clamped to [0, 1] at thresholds; combine as `1 - max(spool, mat)` so no single overlap can drive the weight negative; multiplier feeds the existing dwell-weight pipeline from Slice 7.2; new `boost_penalty_applied: float = 0.0` field on `VeAnalysisCellCorrection` surfaces the total downweighting per cell; defaults preserve Phase 6 baseline; 9 focused tests in `test_ve_analyze_boost_confidence.py` covering multiplier math, default-off no-regression, per-cell penalty surfacing, and vacuum no-penalty symmetry; 1677 tests total
- Phase 7 Slice 7.7 (root-cause diagnostics surface, read-only) — new `VeRootCauseDiagnosticsService` + `RootCauseDiagnostic(rule, severity, message, evidence_cells)` + `RootCauseDiagnosticReport(diagnostics, summary_text, has_findings)` in `ve_root_cause_diagnostics_service.py`; stateless and read-only — never mutates the input `VeAnalysisSummary`; four closed-form rules running independently: `injector_flow_error` (uniform global bias with low variance), `deadtime_error` (low-load/low-rpm region biased meaningfully more than the rest), `target_table_error` (high vs low load biased in opposite directions), `sensor_calibration_error` (Pearson r between row index and correction factor ≥ 0.7); thresholds tuned to fire only on obvious patterns and stay silent on healthy data; below `_MIN_PROPOSALS = 6` the report is empty; 10 focused tests in `test_ve_root_cause_diagnostics.py` covering threshold gating, healthy-data silence, each rule (lean/rich variants and variance suppression for injector flow), and the read-only mutation guarantee; 1687 tests total
- Phase 7 end-to-end pipeline test — `tests/unit/test_phase7_end_to_end_pipeline.py` builds a synthetic 46-record datalog with five clusters (steady cruise, steady boost, transient rpm, spool transition, firmware-marked transient) on a 4×4 VE table that covers vacuum and boost; runs the full VE Analyze pipeline twice, once Phase 6 baseline and once with all seven Phase 7 features enabled, and asserts each slice's signature on the resulting summary: 7.1 firmware learn gate rejects firmware-marked cluster; 7.2 cruise cell clamp from raw 1.10 → effective 1.05 with `clamp_applied=True` and `raw_correction_factor=1.10`; 7.3 16-cell coverage map populated with confidence scores; 7.4 derivative gate rejects transient cluster; 7.5 smoothed layer is byte-separate from the raw `summary.proposals`; 7.6 spool cell carries non-zero `boost_penalty_applied`; 7.7 uniform-lean fixture flagged as `injector_flow_error`. 4 focused tests; 1691 tests total
- Ecosystem cross-repo audit (2026-04-10) — surveyed `C:/Users/Cornelio/Desktop/Airbear-main` (Airbear firmware) and `C:/Users/Cornelio/Desktop/speeduino-202501.6` (Speeduino firmware) and reconciled the Tuner docs against both. **Airbear moved from v0.1.2 → v0.2.0**; key new facts the desktop docs didn't capture: (1) fourth operating mode `CONNECTION_TYPE_DASH_ECHO` running dashboard + TunerStudio TCP simultaneously via UART mutex; (2) TCP bridge returns explicit `RC_TIMEOUT (0x80)` + `RC_BUSY_ERR (0x85)` error bytes and `ECU_SERIAL_TIMEOUT = 400 ms` with no internal retry — desktop `TcpTransport` read path must recognise these as first-class recoverable errors; (3) Dash mode now parses the full 148-byte OCH block (50+ channels including ethanol%, VVT1/VVT2 angles, knock counts, engine-protect status, PW2–6); (4) new REST API `/api/realtime` (with `fw_variant` envelope), `/api/status`, `/api/log/*`; (5) EcuHub UDP auto-discovery on port 21846 broadcasting `Dropbear v2.0.1`; (6) AP-mode fallback uses password `Bear-XXYYZZ` (last 3 MAC octets), not the open network older docs described; (7) OTA firmware endpoints `POST /updateFWUpload` + `/updateDataUpload` + remote-URL fetch; (8) 5-second hardware watchdog that can drop the TCP session on firmware deadlock; (9) CAN bus wired on GPIO 4/5 @ 500 kbps but not yet in the dashboard JSON (preview). **Speeduino firmware** new facts: (1) `runtimeStatusA` bits 5 (`transientActive`) and 6 (`warmupOrASEActive`) are committed — desktop autotune gating that only checks bit 7 accepts invalid transient/warmup samples; (2) board-capability byte at offset 130 now exposes all eight bits including `BOARD_CAP_HIGH_RES_TABLES (bit 5)`, `BOARD_CAP_RTC`, `BOARD_CAP_SD`, `BOARD_CAP_NATIVE_CAN`, `BOARD_CAP_SPI_FLASH`, `BOARD_CAP_12BIT_ADC` — desktop needs a central `BoardCapabilities` reader, not per-call-site bit tests; (3) firmware publishes named `OCH_OFFSET_BOARD_CAPABILITY_FLAGS = 130`, `OCH_OFFSET_FLASH_HEALTH_STATUS = 131`, `OCH_OFFSET_RUNTIME_STATUS_A = 147` constants in `live_data_map.h` — desktop should reference these by name, not as magic numbers; (4) the `CAP_FEATURE_U16P2` feature flag in the `'K'` capability-response plus the `TS_PAGE_SERIALIZATION_CURRENT_BYTES` / `TS_PAGE_SERIALIZATION_NATIVE_U16` enum in `pages.h:17–20` are the authoritative per-page U08/U16 signal — **not** the signature string and **not** `BOARD_CAP_HIGH_RES_TABLES` alone; (5) SD card reality check — only `TS_CMD_SD_FORMAT = 13057` exists as a controller-button command; `cmdSdList` / `cmdSdRead` **do not exist** as raw commands; the G5 post-Phase-14 backlog plan must be revised to go through the existing page/calibration transport or to propose new firmware commands; (6) `LOG_ENTRY_SIZE = 148` remains stable and append-only — future telemetry widenings must not reorder existing rows or the `ChannelContract` replay path breaks historical logs; (7) production signature `speeduino 202501-T41` and experimental `-U16P2` are **still both shipped** — retirement happens as part of Firmware Phase 12, not independently. Results written into `docs/tuning-roadmap.md` as a new "Ecosystem alignment" subsection under the Phase 14 / Phase 12 joint commitment with a 10-item improvement backlog (central BoardCapabilities reader, OCH offset name mirroring in `ChannelContract`, `TcpTransport` error-code handling, unified mDNS+UDP discovery, definition-driven U08/U16 generation, `fw_variant` cross-check, SD plan revision, REST as secondary-observer path, optional Airbear OTA integration, append-only live-data lock). CLAUDE.md updated with Airbear 0.2.0 + `RC_BUSY_ERR` notice. No code changes this turn — pure cross-repo reconciliation so the three projects stay in lockstep before the next round of C++ Phase 14 slices commits to contracts that depend on any of these facts.
- Phase 14 Slice 11 (formula / virtual output channels parser — G4 Phase 1) — added the first parser half of the G4 "virtual / formula output channels" gap. Python side: new `FormulaOutputChannel(name, formula_expression, units, digits)` domain model plus `EcuDefinition.formula_output_channels: list[FormulaOutputChannel]`; `IniParser._parse_output_channels` extended with a dedicated formula branch that matches `name = { expression } [, "units"] [, digits]` *before* the scalar/array/bits pattern so bare-formula lines no longer silently drop. Scalar entries that merely embed a `{ expression }` token in their units/scale/translate slots (e.g. `idleLoad = scalar, U08, 38, { bitStringValue(...) }, ...`) are unaffected because the formula regex requires `=\s*\{` immediately after the name. Inline `;` comments are stripped from formula lines before the regex runs. 14 focused Python tests in `test_formula_output_channels.py` covering the no-metadata form, units-only, units+digits, inline comment strip, ternary expression verbatim preservation, `arrayValue()` nested-paren preservation, coexistence with scalar entries, order preservation, cross-section isolation, and five production-INI assertions (≥30 formula channels, core name set, `map_psi` exact expression, `throttle` `%` units, no name collisions with hardware scalar channels). C++ side: new `IniFormulaOutputChannel(name, formula_expression, units, digits)` on `IniOutputChannelsSection`, matching formula branch in `ini_output_channels_parser.cpp` via `std::regex` (same pattern as Python) with inline `;`-comment stripping. 10 new doctest cases mirroring the Python synthetic set plus a preprocessed-pipeline test that gates a `#if CELSIUS` formula line. nanobind binding extended with `IniFormulaOutputChannel` class and the `formula_channels` accessor on `IniOutputChannelsSection`. 10 new parity tests in `test_cpp_ini_output_channels_parser_parity.py`: 5 production-INI (formula channel name sequence match, byte-identical expression strings, units match, count ≥30, core names present) plus 5 synthetic (names, expressions, units, digits, scalar/formula cross-contamination check). **All 10 parity tests passed on the first build run** against the production INI, including byte-identical expression strings across all 65 formula channels. C++ doctest suite: **1078 tests, 9699 assertions, 0 failures** (+10 new). Full Python suite: **2469 passed** (+24: 14 formula unit tests + 10 formula parity tests). This slice delivers parser/catalog state only — actual evaluation of the expressions against a live channel snapshot (the `MathExpressionEvaluator` half of G4) is deferred to a follow-up slice so the parser contract can stabilize independently of evaluator design decisions. Unblocks any C++-side code that wants to enumerate computed channels for the dashboard, runtime display, or the live-data HTTP API without waiting on the evaluator.
- Phase 14 Slice 10 (native C++ INI [GaugeConfigurations] section parser via nanobind) — new `cpp/include/tuner_core/ini_gauge_configurations_parser.hpp` with `IniGaugeConfiguration` (name, channel, title, units, optional lo/hi/lo_danger/lo_warn/hi_warn/hi_danger as `std::optional<double>`, value_digits, label_digits, optional category) and `IniGaugeConfigurationsSection`. New `cpp/src/ini_gauge_configurations_parser.cpp` directly ports `IniParser._parse_gauge_configurations`. Stateful grammar: `gaugeCategory = "Name"` lines update an active category that subsequent gauge entries inherit; gauge entries with brace-expression thresholds like `{rpmhigh}` resolve to `nullopt` (frozen — can't evaluate at parse time); inline `;` comments stripped; entries with fewer than 3 CSV parts dropped. Composed pipeline `parse_gauge_configurations_section_preprocessed` chains preprocess + parse. 12 doctest cases covering outside-section ignore, single full-form gauge, brace-expression nullopt, gaugeCategory inheritance, no-category entries, short-entry drop, comments/blank lines, inline comments, value_digits/label_digits float-source, multiple gauges, section change leak prevention, preprocessor gating. nanobind binding extended with the two new classes and two new functions. CMakeLists.txt now compiles ten source files into `tuner_core`. 7 Python parity tests in `test_cpp_ini_gauge_configurations_parser_parity.py`: 3 synthetic (count, names, all fields) plus 4 production-INI cases asserting byte-identical parity against `tests/fixtures/speeduino-dropbear-v2.0.1.ini` for the gauge name set, every gauge's channel/title/units/thresholds/digits/category, and total count >= 50. **All 7 parity tests passed on the first build run** — sixth subsystem (after [Constants], [OutputChannels], [TableEditor], [CurveEditor], [Menu]) to achieve full byte equality against the production INI. C++ doctest suite: 128 tests, 416 assertions, 0 failures. Python parity suite: 112/112 passing. Full Python suite: **1917/1917 passing**.
- Phase 14 Slice 9 (native C++ INI [Menu] section parser via nanobind) — new `cpp/include/tuner_core/ini_menu_parser.hpp` with `IniMenuItem` (target, optional label, optional page, optional visibility_expression), `IniMenu` (title + items), and `IniMenuSection`. New `cpp/src/ini_menu_parser.cpp` directly ports `IniParser._parse_menus`. Stateful grammar: `menu = "Title"` opens a new menu, then `subMenu = ...` and `groupChildMenu = ...` lines populate items into the active menu. `std_separator` items are dropped (TS uses these as visual dividers). The page number and `{visibility_expression}` may appear in either order after the label, and either is optional. Composed pipeline `parse_menu_section_preprocessed` chains preprocess + collect_defines + parse. 14 doctest C++ cases covering single menu/single item, subMenu and groupChildMenu both populating items, std_separator drop, label-defaults-to-target, page parsing, visibility expression captured verbatim, page+visibility in either order, multiple menus tracked separately, items-before-menu drop, section change clearing the active menu, comments and blank lines, preprocessor gating, invalid page kept as None without dropping the item. nanobind binding extended with the three new classes and two new functions. CMakeLists.txt now compiles nine source files into `tuner_core`. 14 Python parity tests in `test_cpp_ini_menu_parser_parity.py`: 7 synthetic (menu count, titles, item targets, item labels, item pages, item visibility expressions, std_separator drop) plus 7 production-INI cases that load `tests/fixtures/speeduino-dropbear-v2.0.1.ini` and assert byte-identical parity for the menu count, every menu title, every menu's item target sequence (locks the navigation tree), every item label sequence, every visibility expression (locks how `LAMBDA`-only pages are hidden from the navigator), every item page, and total menu count >= 5. **All 14 parity tests passed on the first build run** — fifth subsystem (after [Constants], [OutputChannels], [TableEditor], [CurveEditor]) to achieve full byte equality against the production INI. Combined with the four prior section parsers, the C++ tree now has every piece of metadata it needs to render the operator's complete tuning navigation tree: scalars/tables ([Constants]), live data block schema ([OutputChannels]), 3D map editor metadata ([TableEditor]), 1D curve editor metadata ([CurveEditor]), and now the navigation tree referencing all of the above ([Menu]). C++ doctest suite: 116 tests, 369 assertions, 0 failures. Python parity suite: 105/105 passing. Full Python suite: **1910/1910 passing**.
- Phase 14 Slice 8 (native C++ INI [CurveEditor] section parser via nanobind) — new `cpp/include/tuner_core/ini_curve_editor_parser.hpp` with `CurveYBins` (param + optional label), `CurveAxisRange` (min/max/steps), `IniCurveEditor` (full mirror of Python `CurveDefinition`: name, title, x_bins_param, x_channel, y_bins_list, x_label, y_label, x_axis, y_axis, topic_help, gauge, size as `std::array<int, 2>`), and `IniCurveEditorSection`. New `cpp/src/ini_curve_editor_parser.cpp` directly ports `IniParser._parse_curve_editors`. Stateful grammar with two extra wrinkles vs `[TableEditor]`: **multi-line curves** accumulate multiple `yBins` entries (WUE Analyze "current vs recommended" pattern), and **`lineLabel` lines** may appear before, after, or interleaved with the `yBins` — they're collected into a `pending_line_labels` buffer and matched onto the y_bins_list positionally at flush time. Section change flushes the active curve before resetting. Title defaults to name when missing. Inline `; comment` stripping. Numeric fields validate before assignment (xAxis needs all 3 components, size needs both width and height). Composed pipeline `parse_curve_editor_section_preprocessed` chains preprocess + collect_defines + parse. 14 doctest C++ cases covering single curve with all fields, multiple curves with flush, multi-line curves, lineLabel positional matching, lineLabel tail-positioned, title default, section change flush, inline comments, malformed xAxis, malformed size, comments and blank lines, preprocessor gating, final-curve-at-EOF flush. nanobind binding extended with the four new classes and two new functions. CMakeLists.txt now compiles eight source files into `tuner_core`. 12 Python parity tests in `test_cpp_ini_curve_editor_parser_parity.py`: 5 synthetic (count, names, fields, y_bins lists, axis ranges) plus 7 production-INI cases that load `tests/fixtures/speeduino-dropbear-v2.0.1.ini` (34+ curves) and assert byte-identical parity for the curve set, every curve's x_bins_param, every curve's y_bins_list params, every curve's y_bins_list labels (multi-line curve legends), every curve's x_channel (operating-point cursor on curves), every curve's title, and total count >= 30. **All 12 parity tests passed on the first build run** — fourth subsystem (after [Constants], [OutputChannels], [TableEditor]) to achieve full byte equality against the production INI. Combined with [TableEditor], the C++ tree now has metadata for every editable surface in the tuning workspace — 3D maps and 1D curves both. Unblocks the C++ curve editor widget, the curve-aware generators (WUE/cranking/ASE), the operating-point cursor on curves (G3 extended to curves), and Hardware Setup wizard preset application. C++ doctest suite: 102 tests, 327 assertions, 0 failures. Python parity suite: 91/91 passing. Full Python suite: **1896/1896 passing**.
- Phase 14 Slice 7 (native C++ INI [TableEditor] section parser via nanobind) — new `cpp/include/tuner_core/ini_table_editor_parser.hpp` with `IniTableEditor` (`table_id`, `map_id`, `title`, `page`, `x_bins`, `y_bins`, `z_bins`, `x_channel`, `y_channel`, `x_label`, `y_label`, `topic_help`, `grid_height`, `grid_orient` as `std::array<double, 3>`, `up_label`, `down_label`) and `IniTableEditorSection` (vector of editors). New `cpp/src/ini_table_editor_parser.cpp` directly ports `IniParser._parse_table_editors` — stateful grammar where `table = ...` opens new editors and subsequent key=value lines populate fields on the active editor until the next `table =` or section change. Section change clears the active editor (mirrors Python). Handles `table` (with optional page), `topicHelp` (with quote stripping and trailing comment removal), `xBins` / `yBins` (with optional channel), `zBins`, `xyLabels`, `gridHeight`, `gridOrient` (3-tuple required), `upDownLabel`. Composed pipeline `parse_table_editor_section_preprocessed` chains preprocess + collect_defines + parse. 13 doctest C++ cases covering single editor with all fields, multiple editors, xBins without channel, malformed `table` line drop, keys-before-table-line drop, section change clears state, comments, unquoted topicHelp, topicHelp with trailing comment, gridOrient with fewer than 3 values, gridHeight non-numeric, preprocessor gating. nanobind binding extended with the two new classes and three new functions; `nanobind/stl/array.h` added for the `std::array<double, 3>` binding on `grid_orient`. CMakeLists.txt now compiles seven source files into `tuner_core`. 11 Python parity tests in `test_cpp_ini_table_editor_parser_parity.py`: 5 synthetic (editor count, table_id set, all fields, grid_height + grid_orient, topic_help) plus 6 production-INI cases that load `tests/fixtures/speeduino-dropbear-v2.0.1.ini` and assert byte-identical parity for the editor set, every editor's z_bins (the headline claim — what generators and runtime consumers reference), every editor's axis bins, every editor's axis channels (which feeds the future G3 operating-point overlay), every editor's title, and total count >10. **All 11 parity tests passed on the first build run** — third subsystem (after [Constants] and [OutputChannels]) to achieve full byte equality against the production INI. Unblocks Phase 14 Slice 7 (table generators reading z_bins as the write target), Slice 8 (2D table editor widget reading display labels/help/axis channels), Slice 9 (3D table surface G2 reading grid_height/grid_orient), and the operating-point crosshair G3 (reading x_channel/y_channel). C++ doctest suite: 88 tests, 270 assertions, 0 failures. Python parity suite: 79/79 passing. Full Python suite: **1884/1884 passing**.
- TunerStudio feature gap analysis added to roadmap — Surveyed `C:/Users/Cornelio/Desktop/Decompiled/TunerStudioMS/` to identify missing features compared to TunerStudio. Two user-requested items committed to Phase 14: **(G1) `StartupSurface`** that runs before the workspace tabs show — recent projects list, "New Project" wizard, "Hardware Setup Wizard" launcher, "Connect & Auto-Detect" quick action; based on `aP/f.java` / `R/b.java` / `R/j.java` in the decompiled tree; replaces the current "open last project automatically" behaviour but keeps that as a checkbox option. **(G2) `TableSurface3DView`** — 3D rotatable mesh visualization for tables with mouse rotation, elevation/azimuth sliders, and a live operating-point marker on the surface; based on `com/efiAnalytics/ui/bt.java` (rotation controls JSlider azimuth 0-359° / elevation 15-90° / "Follow Mode" checkbox) and `G/bi.java` (3D table data model); implementation via Qt 3D module (`Qt3DCore` + `Qt3DRender` + `Qt3DExtras`) for the C++ build, with an interim Python prototype using pyqtgraph or matplotlib's `mpl_toolkits.mplot3d` so the operator gets the feature now and the C++ port has a reference. **G3 also folded in**: live operating-point crosshair overlay on the 2D table editor (the data path already exists via `TableReplayContextService` — only the paint operation is missing). Phase 14 slice plan extended: Slice 8 (UI build) gains the `StartupSurface`; new Slice 9 added for the 3D table surface as a dedicated visualization investment; Slices 10/11 are now Native format canonicalization and Python retirement (renumbered from 9/10). Post-Phase-14 polish backlog added at the bottom of `docs/tuning-roadmap.md` tracking the lower-priority gap items: **G4** virtual/formula output channels (`{expression}` form on `IniOutputChannel`, extend `VisibilityExpressionService` into a full `MathExpressionEvaluator`), **G5** SD card log download/browse/replay (`SdCardPanel` in Logging tab + firmware-side `cmdSdList`/`cmdSdRead` commands), **G6** standalone log viewer parity (`LogAnalyzerPanel` with timeline scrubber, multi-cursor, dual-log compare), **G7** ignition timing scope (real-time trigger logger visualization), and the niche/deferred items G8-G12 (dyno view, Bluetooth, multi-tune slots, knock listener, cloud tune sharing) with rationale for each. Each gap entry cites the decompiled-source class file path so the C++ port has a reference. CLAUDE.md updated with a notice paragraph. No code changes — pure roadmap planning.
- Joint U16 commitment refined to "where precision matters" — the original "all 3D maps → U16" framing was too broad. Updated both `C:/Users/Cornelio/Desktop/speeduino-202501.6/speeduino/FIRMWARE_ROADMAP.md` Phase 12 and `docs/tuning-roadmap.md` Phase 14 / Firmware Phase 12 joint commitment to reflect a **selective** widening: only `veTable`/`veTable2`, `afrTable`/`lambdaTable`, `ignitionTable`, `boostTable`, and `vvtTable`/`vvt2Table` widen to U16 because U08 quantization causes visible drivability or tuning-resolution issues there. **Tables that stay U08 even on DropBear**: `wueTable`, `aseTaperTable`/`aseLoadTable`, `crankingEnrichTable`, `injOpenTable`, `iatRetardTable`/`cltAdvanceTable`, flex trims, EGO bounds, dwell compensation, idle/boost PID gains, sensor calibration pages 0/1/2 (which use a separate transport entirely). Quantitative justification table added to both roadmaps showing the per-step granularity gap (e.g. 1% VE per step in U08 vs 0.0015%/step in U16; 0.058 AFR/step vs 0.00023 AFR/step; 0.35° spark advance/step vs 0.0014°/step; 1 kPa boost/step vs 0.004 kPa/step). Pages can now be **mixed** (e.g. a page containing the VE table plus a small WUE-style trim becomes one widened entry plus several byte-mode entries) — that's the new edge case the experimental U16P2 work didn't exercise (it touched all of page 2). Slice plan updated to highlight mixed-page CRC and SPI flash mirror handling as a specific validation target. Desktop-side coordinated decisions also refined: the C++ generator no longer "detects DropBear-class boards"; instead it reads each table's `data_type` from the active `NativeDefinition` and respects whatever the definition says — definitions for DropBear declare the high-leverage tables as U16 and the rest as U08, AVR definitions declare every table as U08, so the same generator code handles every case without branching. Selective migration on load: only the tables that widened get upcast (zero-pad high byte → U16); tables that stayed U08 are bit-identical on disk. Five "why selective is better than blanket" reasons documented: smaller firmware delta, smaller backwards-compat surface, faster ship date, honest characterization, and forward-compatibility with potential future per-table widening on AVR. CLAUDE.md notice updated. No code changes — pure roadmap refinement.
- Joint U16-by-default commitment with Speeduino Firmware Phase 12 — added a new **Phase 12** section to `C:/Users/Cornelio/Desktop/speeduino-202501.6/speeduino/FIRMWARE_ROADMAP.md` committing the DropBear/Teensy firmware to **native U16 maps as the default** for all 3D fueling/ignition/etc. tables. Hard rules: DropBear/Teensy only (AVR keeps byte contract); single production signature `speeduino 202501-T41` (experimental `-U16P2` retired); single firmware build per board family; the 148-byte `LOG_ENTRY_SIZE` packet is unchanged (hi-res telemetry stays in Phase 13B). 8-step slice plan covering page inventory, promotion of the experimental U16P2 page-2 path to all pages, INI/MSQ regeneration, migration path for legacy U08 tunes, test guard updates, hardware validation, and `-U16P2` retirement. **Coordinated desktop-side decisions** mirrored into `docs/tuning-roadmap.md` Future Phase 14 as a new "Phase 14 / Firmware Phase 12 joint commitment" subsection: capability-driven table generation (Hardware Setup wizard / VE/AFR/spark generators consult board capability bits → DropBear gets native U16 tables, AVR gets byte-mode); native definition format encodes data type per `NativeTable`; no legacy U08 generation path on DropBear once both phases ship; on-load migration of legacy U08 tunes (zero-pad high byte → U16, no operator prompt); live telemetry packet unchanged. Sequencing: desktop generators land first (in progress, Slice 7 in the Phase 14 plan) → firmware ships → joint hardware validation. New Slice 7 added to the Phase 14 desktop plan: "Port the table generators with U16-aware output". CLAUDE.md updated with the joint-commitment notice. Firmware roadmap cross-reference table in `docs/tuning-roadmap.md` updated with the new Firmware Phase 12 row. No code changes this turn — pure roadmap coordination so the two projects stay in lockstep before the next round of Phase 14 slices commits to a contract that depends on this decision.
- Phase 14 Slice 6 (native C++ INI [OutputChannels] section parser via nanobind) — new `cpp/include/tuner_core/ini_output_channels_parser.hpp` with `IniOutputChannel` (name, data_type, offset, units, scale, translate, min/max, digits, bit_offset, bit_length, options) and `IniOutputChannelsSection` (channels + arrays map). New `cpp/src/ini_output_channels_parser.cpp` directly ports `IniParser._parse_output_channels`: scalar entries, bits entries with `$macroName` option-list expansion via the slice 5 defines collector, array entries (recorded by name only, not promoted into channels), `defaultValue = name, v0 v1 ...` lines populating the arrays map. Composed pipeline `parse_output_channels_section_preprocessed` chains preprocess + collect_defines + parse — first slice composition for an output-channels-side parser. 11 doctest C++ cases covering scalar/translate/bits/array semantics, `defaultValue` population, unknown-array drop, comments, multi-section document, preprocessor gating, `$macroName` expansion. nanobind binding extended with the two new types and three new functions. CMakeLists.txt now compiles all six source files into `tuner_core`. 9 Python parity tests in `test_cpp_ini_output_channels_parser_parity.py`: 4 synthetic (scalar set, offsets + types, array values, bit options) plus 5 production-INI cases that load `tests/fixtures/speeduino-dropbear-v2.0.1.ini` and assert byte-identical parity for the channel set, every channel offset, every channel data type, every `defaultValue` array Python recognized, and the channel count being >100. **All 9 parity tests passed on the first run** — including the headline production-INI byte-identical claim. This is the second subsystem (after `[Constants]`) that achieves full byte equality against the real production INI through the composed pipeline. C++ doctest suite: 75 tests, 222 assertions, 0 failures. Python parity suite: 68/68 passing. Full Python suite: **1873/1873 passing**. The live-data-block schema is now available in C++ — prerequisite for porting the runtime telemetry decoder, dashboard, and live VE Analyze pipeline.
- Future Phase 14 first milestone (native Qt 6 desktop app target) — the repo pivoted from "Python app + C++ shared core" (Phase 13 gradual port model) to "native C++ Qt 6 desktop app" (Phase 14 full native model). Trigger: operator decision to optimise for performance on constrained hardware. New `cpp/app/main.cpp` with a minimal `QMainWindow` showing the Phase 14 milestone heading. `cpp/CMakeLists.txt` extended with a `TUNER_BUILD_APP` option (default OFF) that adds a `tuner_app` executable linking `tuner_core` + `Qt6::Core` + `Qt6::Gui` + `Qt6::Widgets`. `Qt6::EntryPoint` interface dependency stripped from `Qt6::Core` to work around a UCRT-vs-MSVC-runtime mismatch (the prebuilt Qt 6.7 entry-point library exports `__imp___argc` which UCRT-based MinGW 15.2.0 doesn't); app built as console-subsystem in v1 so plain `int main(int argc, char* argv[])` links cleanly (promotes to GUI subsystem with a `WinMain` shim in a later slice). Qt 6.7.3 installed via `pip install aqtinstall && python -m aqt install-qt windows desktop 6.7.3 win64_mingw --outputdir C:/Qt` (~18 seconds, ~250 MB). `tuner_app.exe` is **345 KB** static-linked against `libtuner_core.a` and dynamic-linked against `Qt6Core.dll` / `Qt6Gui.dll` / `Qt6Widgets.dll` / `platforms/qwindows.dll`. Verified to launch the Qt event loop and run for the full 3-second test window with no crash. Locked Phase 14 scope decisions written into `docs/tuning-roadmap.md`: full native end goal, JSON for both native formats (JSON5 deferred), Qt 6.7 LGPL dynamic-linked, hand-coded C++ widgets (no `.ui`/QML), Python retired after feature parity in one PR, single-binary distribution. Phase 13 prior work (5 parser slices, `tuner_core` static lib, doctest suite, parity harness) all carries over verbatim — the pivot only adds the `tuner_app` target alongside, doesn't invalidate any prior work. `cpp/README.md` rewritten with the Phase 14 build instructions (`aqtinstall` setup, CMake configure with `CMAKE_PREFIX_PATH=C:/Qt/6.7.3/mingw_64`, Qt DLL copy step). Full Python suite still **1864/1864 passing**, C++ doctest suite still **64/64 passing**, parity suite still **59/59 passing** — the Phase 14 pivot is purely additive at this point.
- Future Phase 13 fifth slice (native C++ INI defines collector + `$macroName` expander) — new `cpp/include/tuner_core/ini_defines_parser.hpp` with `IniDefines = map<string, vector<string>>`, `collect_defines(text)`, `collect_defines_lines(lines)`, `expand_options(parts, defines)`. Direct port of `IniParser._collect_defines` and `_expand_options` (recursion capped at 10 levels, `{expression}` dropped, unresolved `$name` silently dropped). New `cpp/src/parse_helpers.hpp` private header hoists the shared `strip`/`strip_quotes`/`parse_csv` helpers into `tuner_core::detail` so both `ini_constants_parser.cpp` and `ini_defines_parser.cpp` consume them; refactored the constants parser to drop its private duplicates (~40 lines removed). 12 doctest C++ cases covering single/multiple defines, mixed-section parsing, malformed `#define noEquals`, single-value defines, simple/recursive/nested/unresolved/circular `$macro` expansion, `{expression}` drop, empty-token skip. nanobind binding extended with `collect_defines` and `expand_options` plus a new `defines` parameter on `parse_constants_section` and `parse_constants_lines` (default-empty for backwards compatibility). `parse_constants_section_preprocessed` now automatically collects defines from the preprocessed text and wires them in for bit-option expansion — first **three-slice internal composition** (preprocessor + defines + constants in one entry point). 12 Python parity tests in `test_cpp_ini_defines_parser_parity.py`: synthetic defines (4), production INI defines (3 — full set equality + byte-identical token lists + sanity), `expand_options` parity against `IniParser._expand_options` (4), and **the headline composed-pipeline test** that asserts bit-option labels on the production INI match Python byte-for-byte after `$macroName` expansion across 5+ overlapping bit scalars. **Two parity bugs caught and fixed during validation**: (1) shared `strip_quotes` was checking for *paired* quotes but Python's `str.strip('"')` strips any leading/trailing `"` independently (matters for production option lists like `"No correction" ; * (1 byte)` that span trailing `;` comments) — ported Python's character-set strip semantics directly; (2) the constants parser was calling `strip()` on each option *after* `expand_options` returned, eating trailing whitespace inside quoted labels like `"Relative "` — Python doesn't do this, removed the extra strip. C++ doctest suite: 64 tests, 179 assertions, 0 failures. Python parity suite: 59/59 passing. Full Python suite: **1864/1864 passing**, no skips. Phase 13 status: 5 of 5 originally-planned foundation slices complete and validated.
- Phase 13 Slice 4 follow-up: composed pipeline (`parse_constants_section_preprocessed`) — wired the existing Slice 3 preprocessor into the Slice 4 constants parser via a new entry point that calls `preprocess_ini_text` first then forwards the surviving lines to `parse_constants_lines`. First proven slice composition in the C++ tree, mirrors the Python `IniParser.parse()` flow exactly. 3 new doctest cases covering preprocessor-disabled `#if`-only scalars, preprocessor-enabled scalars when `active_settings` matches, and file-scope `#set` defaults steering `#if`/`#else` branches. **Production INI parity strengthened from subset to exact equality**: the 5 production-INI parity tests now assert the C++ scalar/array catalogs exactly equal the Python `[Constants]`-sourced subset (Python entries with non-None `page` and `offset`); the 20-scalar gap to full `EcuDefinition.scalars` is correctly identified as non-`[Constants]`-sourced (from `[Menu]`/`[Tuning]`/`[SettingGroups]` etc., which the v1 C++ parser doesn't handle). C++ doctest suite: 52 tests, 144 assertions, 0 failures. Python parity suite: 47/47 passing. Full Python suite: **1852/1852 passing**, no skips. The C++ `[Constants]` parser is **exact** for the section it claims to handle.
- Future Phase 13 fourth slice (native C++ INI [Constants] section parser via nanobind) — new `cpp/include/tuner_core/ini_constants_parser.hpp` with `IniScalar`, `IniArray`, `IniConstantsSection` dataclasses plus `parse_constants_section(text)` and `parse_constants_lines(lines)` overloads. New `cpp/src/ini_constants_parser.cpp` directly ports `IniParser._parse_constant_definitions`: page tracking, scalar/array/bits/string entries, `lastOffset` auto-advance with per-page reset, 1D `[N]` and 2D `[NxM]` array shapes, single-bit `[N]` and bit-range `[start:end]` bit shapes (with start/end auto-swap), `_parse_csv` port that respects quoted strings + brace expressions + parenthesized groups, `_parse_value_token` / `_parse_float_token` / `_parse_int_token` / `_resolve_constant_offset` / `_constant_storage_size` / `_parse_shape` / `_parse_bit_shape` all ported. Storage widths: U08/S08=1, U16/S16=2, U32/S32/F32=4. 15 doctest C++ cases covering scalar parse, lastOffset width semantics, page-boundary reset, 2D and 1D array shapes, array storage size, bit range and single-bit shapes, comments/blank lines, hex literals, multi-page documents, brace placeholder tolerance. nanobind binding extended with `IniScalar`/`IniArray`/`IniConstantsSection` classes and `parse_constants_section`/`parse_constants_lines` functions. CMakeLists.txt now compiles all four source files into `tuner_core`. 11 Python parity tests in `test_cpp_ini_constants_parser_parity.py`: 7 synthetic-fixture cases (scalar count, array count, offsets+pages, lastOffset resolution at offset 272 after a 16×16 U08 array, 2D/1D dimensions, units+types, page-boundary reset) plus **4 real production INI cases** that load `tests/fixtures/speeduino-dropbear-v2.0.1.ini` (the 15-page production INI used by 50+ existing Python round-trip tests) and assert: scalars parsed by C++ are a subset of the Python catalog with byte-identical pages/offsets, arrays parsed by C++ are a subset with matching dimensions, ≥50 scalars and ≥10 arrays found. Subset relationship rather than equality because v1 doesn't yet do `[Defines]` expansion or `#if` preprocessing — but the subset is exact for the entries it does parse. **Validated end-to-end against the real production INI on the first run** — strongest correctness signal Phase 13 has shipped because prior slices used synthetic fixtures only. C++ doctest suite: 49 tests, 139 assertions, 0 failures. Python parity suite: 46/46 passing. Full Python suite: **1851/1851 passing**, no skips when the extension is built. Phase 13 status: 4 of 4 planned foundation slices complete and validated.
- Phase 13 first build validation pass — built and validated end-to-end on Windows (MinGW g++ 15.2.0 + Python 3.14 + nanobind 2.12.0). C++ doctest suite: 34 tests, 80 assertions, 0 failures. Python parity suite: **all 35 parity tests passing** against the built `tuner_core.cp314-win_amd64.pyd`. Three fixes during validation: (1) `cpp/CMakeLists.txt` static-links the MinGW runtime (`-static-libgcc -static-libstdc++ -static -lwinpthread`) so the resulting `.pyd` has zero runtime DLL dependency — Python on Windows doesn't honor `PATH` for DLL search since 3.8 and `os.add_dll_directory()` calls before every import would have been a developer trap; (2) the `preprocess_ini_lines` / `preprocess_ini_text` bindings switched from raw function pointers to lambdas accepting `std::string` (not `std::string_view`) so nanobind autoconverts from Python `str` without needing `nanobind/stl/string_view.h`; (3) the byte-equality MSQ writer parity test was rewritten as a semantic-equality test because Python and C++ take two different valid writer strategies — Python re-serializes via `xml.etree.ElementTree` while C++ byte-splices the source, and the C++ approach is strictly better for round-trip fidelity (preserves verbatim formatting outside the edited regions). Updated `cpp/README.md` with the validated MinGW build commands and the static-link rationale. Full Python suite: **1840 / 1840 passing**, no skips when the extension is built; 35 cleanly skipped when not. Phase 13 first three slices are now end-to-end validated against the Python oracle.
- Future Phase 13 third slice (native C++ INI preprocessor via nanobind) — new `cpp/include/tuner_core/ini_preprocessor.hpp` and `cpp/src/ini_preprocessor.cpp` directly porting `tuner.parsers.common.preprocess_ini_lines`. Two-pass evaluation: phase 1 collects file-scope `#set`/`#unset` defaults (only at file scope, ignoring directives nested inside `#if` blocks); phase 2 walks lines applying conditional logic with `effective = file_defaults | active_settings`. Stack-based nesting, `#else` flips inside parent active context, hash-prefixed comments dropped only when inside an inactive branch, `#set`/`#unset` consumed not emitted. New `preprocess_ini_text` overload normalizes `\r\n`/`\r`/`\n` line endings to match Python's `text.splitlines()`. 16 new doctest C++ cases in `cpp/tests/test_ini_preprocessor.cpp` mirroring the Python `test_ini_preprocessor.py` suite case-for-case (plain lines unchanged, `#if`-set keep/drop, `#else` taken/suppressed, file-scope `#set` default, user-additive `active_settings`, nested both-active/outer-only/outer-inactive, `#set`/`#unset` consumed, active/inactive comment handling, empty-line preservation, CRLF normalization). nanobind binding extended with `nanobind/stl/set.h` so `active_settings` accepts a Python `set[str]`; binds both `preprocess_ini_lines` and `preprocess_ini_text` with default-empty `active_settings`. CMakeLists.txt now compiles all three source files into `tuner_core`. cpp/README.md tree updated and expected test count bumped to 34 (8 MSQ + 10 NativeFormat + 16 INI preprocessor). 16 new Python parity tests in `test_cpp_ini_preprocessor_parity.py` using a parametrized harness over 13 fixtures (one per Python preprocessor test case shape plus a real Speeduino INI subset) that asserts byte-identical output between Python and C++ for every fixture, plus 3 standalone tests verifying lines-overload == text-overload equivalence, default-empty `active_settings` == explicit empty set, and a multi-section nested-`#if` Speeduino INI fragment round trip. Full suite now: 1805 passed + 35 skipped (9 MSQ parity + 10 NativeFormat parity + 16 INI preprocessor parity, all awaiting `cmake --build`)
- Future Phase 13 second slice (native C++ NativeFormat JSON layer via nanobind) — new `cpp/include/tuner_core/native_format.hpp` with `NativeDefinition`, `NativeTune`, `NativeParameter`/`Axis`/`Table`/`Curve`, `NativeFormatVersionError`; new `cpp/src/native_format.cpp` using vendored `nlohmann/json.hpp` (single header at `cpp/third_party/nlohmann/json.hpp`) with `nlohmann::ordered_json` so emitted field order matches Python `dataclasses.asdict()`; schema-version gating mirrors `NativeFormatService._check_version` byte-for-byte (missing → throws, future major → throws, forward-compat minor → accepted, unparsable → throws); 10 new doctest C++ cases in `cpp/tests/test_native_format.cpp` covering dump emit, structural round trip, tune variant types (scalar / list / string), and the full schema-version gating matrix; nanobind binding extended with `nanobind/stl/optional.h` and `nanobind/stl/variant.h`, exposing all dataclasses + `dump_definition`/`dump_tune`/`load_definition`/`load_tune`/`load_definition_file`/`load_tune_file`; CMakeLists.txt now compiles both `msq_parser.cpp` and `native_format.cpp` into the `tuner_core` static library and fail-fast checks for `cpp/third_party/nlohmann/json.hpp` with a clear download URL; cpp/README.md updated with the second `curl` instruction and bumped expected test count to 18 (8 MSQ + 10 NativeFormat); 10 new Python parity tests in `test_cpp_native_format_parity.py` using the same gating model as the MSQ parity suite, covering dump-definition output equality, Python↔C++ load round-trip in both directions for definitions and tunes (with variant types — scalar / list / string), and missing/future/minor schema-version gating in both directions; full suite now: 1805 passed + 19 skipped (9 MSQ parity + 10 NativeFormat parity, both awaiting `cmake --build`)
- Future Phase 13 first slice (native C++ MSQ parser via nanobind) — new `cpp/` subdirectory of the repo. C++20 + CMake + stdlib-only (`tuner_core` static library), doctest unit suite, nanobind binding exposing `parse_msq`/`parse_msq_text`/`write_msq`/`write_msq_text` plus `MsqDocument`/`MsqConstant` dataclasses. Hand-rolled XML scanner in `cpp/src/msq_parser.cpp` (~150 lines) avoids pulling in pugixml/expat. New `cpp/CMakeLists.txt` with `TUNER_CORE_BUILD_TESTS` (default ON) and `TUNER_CORE_BUILD_BINDINGS` (default OFF) options; doctest vendored at `cpp/third_party/doctest/doctest.h` with a fail-fast CMake message linking to the download URL when missing. Phase 13 scope decisions locked into `docs/tuning-roadmap.md` (gradual replacement, MSQ parser first, strict Python-as-oracle, C++20, MSVC + CMake + stdlib + nanobind + doctest, `cpp/` subdirectory, cibuildwheel + Python fallback). 8 doctest C++ unit cases covering parse, byte-stable no-op write, scalar update, multi-line table replacement, unknown-name drop parity, and malformed input. 9 Python parity tests in `tests/unit/test_cpp_msq_parser_parity.py` that import `tuner_core` from any in-tree CMake build directory or installed wheel and assert: parse signature parity, parse constant-set parity, parse table-shape parity, no-op write byte stability, scalar update round trip through Python parser, **byte-for-byte equality between Python `MsqWriteService.save()` and C++ `write_msq()` output** (the core parity claim), unknown-constant drop parity, and two production-MSQ-fixture cases. The parity suite is **gated on the C++ extension being importable** — dev installs without a compiler skip rather than fail. Build instructions in `cpp/README.md`. Full suite now: 1805 passed + 9 skipped (the new parity suite, awaiting `cmake --build`) — `WidebandCalibrationPanel` is now actually instantiated by `MainWindow._build_runtime_tab()` and placed in the Runtime tab next to `HardwareTestPanel`; new `MainWindow._send_wideband_calibration(page, payload)` method routes the panel's Apply through `getattr(client, 'write_calibration_table')` with a no-op guard when no client is active and a clear status-bar message on success/failure; connect / disconnect hooks toggle `set_connected()` symmetrically with `HardwareTestPanel`; 5 offscreen Qt tests in `test_main_window_wideband_wiring.py` use a deterministic `main_window` pytest fixture that constructs + closes + drains pending events on teardown so downstream Qt tests stay order-independent; tests cover panel construction, default-disabled state, full payload dispatch verified against the service-generated bytes (preset → service.generate() → encode_payload() == panel-routed payload), no-op when no active client, and disconnect-disable; closes the wideband UI follow-up that was left as orphaned panel code in an earlier slice; 1805 tests total
- Future Phase 12 deliverable 4 (ChannelContract from live_data_map.h) — new `tuner.domain.channel_contract` module with `ChannelEncoding(U08, U08_BITS, U16_LE, S16_LE, U32_LE)`, `ChannelEntry(name, byte_start, byte_end, readable_index, encoding, field, notes, locked)`, `ChannelContract(log_entry_size, firmware_signature, entries, runtime_status_a_offset, board_capability_flags_offset, flash_health_status_offset, schema_version)`; new `LiveDataMapParser` in `services/live_data_map_parser.py` consumes the real firmware `live_data_map.h` header from `C:/Users/Cornelio/Desktop/speeduino-202501.6/speeduino/live_data_map.h`; line-based regex parser tolerates `[LOCKED]` / `[low]` / `(Phase N)` markers and skips unknown rows; extracts `LIVE_DATA_MAP_SIZE` constant and `OCH_OFFSET_*` special-offset constants; `ChannelContract.find(name)` and `find_by_byte(byte_index)` lookup helpers; 16 focused tests in `test_live_data_map_parser.py` including a **structural canary** that sums every parsed entry width against the real firmware file's `LIVE_DATA_MAP_SIZE = 148` so any future schema bump breaking the parser fails loudly; closes Future Phase 12 deliverable 4 — **all four Future Phase 12 deliverables are now complete**; 1800 tests total
- Future Phase 12 v1 reverse path (NativeTune → TuneFile → MSQ write) — `NativeFormatService.to_tune_file(native_tune, native_definition)` projects a `NativeTune` back into a legacy `TuneFile` ready for `MsqWriteService.save()`; semantic-id lookup table built from parameters/axes/tables/curves with legacy-only fall-through; lists inherit `rows`/`cols`/`units` from the native model and default to 1×N when no native shape exists; 5 focused unit tests + 1 end-to-end fixture test that drives a synthetic source MSQ through `NativeTune → to_tune_file → MsqWriteService.save → MsqParser.parse` and verifies the bytes round-trip exactly; closes the reverse half of Future Phase 12 deliverable 3; 1784 tests total
- Future Phase 12 v1 (Owned Tune And Definition Contracts) — first concrete deliverables landed: new `tuner.domain.native_format` module with `NativeDefinition(schema_version, name, firmware_signature, parameters, axes, tables, curves)`, `NativeTune(schema_version, definition_signature, values)`, plus `NativeParameter` (kind = scalar/enum/bits), `NativeAxis`, `NativeTable`, `NativeCurve` dataclasses; new `NativeFormatService` in `services/native_format_service.py` projects legacy `EcuDefinition` → `NativeDefinition` (axis-shaped `*Bins` tables collapse into `NativeAxis`, scalars with options become `enum` kind), projects legacy `TuneFile` → `NativeTune` (flat semantic-id-keyed dict, legacy-only values pass through under their legacy name), and serializes both via plain JSON (`NATIVE_SCHEMA_VERSION = "1.0"`); `NativeFormatVersionError` raised on missing/future-major/unparsable schema versions, forward-compatible minor bumps accepted; v1 uses legacy names as semantic ids and defers JSON5 + reverse MSQ export + capability assertions + edit history to v2; 18 focused tests in `test_native_format_service.py` covering legacy projection, tune projection, JSON round trip, schema-version gating, and end-to-end legacy → native → JSON → native; closes Future Phase 12 deliverables 1+2 and the import half of deliverable 3; 1778 tests total
- Phase 8 final closeout (label widget kind / TSDash DashLabel parity) — new optional `text: str | None = None` field on `DashboardWidget`; `GaugeWidget._refresh_internal()` builds a `QLabel` with `dashLabel` styling for `kind == "label"` widgets, falling back to `title` when `text` is `None`; `update_value()` is a no-op for label kind so live telemetry never overwrites the static text; `DashboardLayoutService._widget_to_dict` / `_widget_from_dict` round-trip the new field; 8 focused tests in `test_dashboard_label_widget.py` covering the domain default, text vs title fallback, no-op value update, the swap-from-other-kind via `update_def`, and JSON layout round trip; closes the last Phase 8 sub-item (`HtmlDisplay` deliberately out of scope — needs HTML renderer); Phase 8 promoted to Complete; 1760 tests total
- TSDash `.dash` file service (real TSDash schema) — `TsDashFileService` in `ts_dash_file_service.py` now handles the real TSDash format reverse-engineered from `C:/Users/Cornelio/Desktop/TSDash_decompiled/TSDash/T/c.java` and `T/e.java`: root `<dsh xmlns="http://www.EFIAnalytics.com/:dsh">`, `<bibliography>`, `<versionInfo fileFormat="3.0" firmwareSignature=...>`, `<gaugeCluster>` with cluster background/aspect attributes, and `<dashComp type="com.efiAnalytics.apps.ts.dashboard.{Gauge,Indicator,DashLabel,HtmlDisplay}">` children whose tag names match the JVM reflection-emitted getter names (`RelativeX/Y/Width/Height`, `Title`, `Units`, `Min`, `Max`, `OutputChannel`, `LowWarning`/`HighWarning`/`LowCritical`/`HighCritical`); FQN → kind map (`Gauge → "dial"`, `Indicator → "indicator"`, `DashLabel → "label"`, `HtmlDisplay → "html"`) with simple-classname fallback for unknown types; warning + critical thresholds synthesized into two `GaugeColorZone`s on import; layout name reads from `firmwareSignature`; export emits namespaced `<dsh>` with TSDash-compatible bibliography/versionInfo/cluster/dashComp tree; 18 focused tests in `test_ts_dash_file_service.py` covering parse-real, error cases, export shape, and full round-trip; 1752 tests total
- WidebandCalibrationPanel widget — new `WidebandCalibrationPanel` in `wideband_calibration_panel.py` wraps `WidebandCalibrationService`: preset combo populated from the 5 built-in presets, summary label that reflects the selected preset's voltage→AFR endpoints and notes, Apply button gated on connection state; takes a `send_calibration` callable so the host (MainWindow) routes the resulting 64-byte payload through `client.write_calibration_table(2, payload)` without the panel touching transports directly; selected preset exposed via `selected_preset()`; 6 offscreen Qt tests in `test_wideband_calibration_panel.py` covering preset listing, disconnect-disable guard (and explicit `_on_apply` no-op when disconnected), `set_connected` toggle, dispatch on apply with `WidebandCalibrationPage.O2 == 2`, and summary refresh on preset change; closes the wideband calibration UI gap matrix row down to one-line MainWindow wiring; 1749 tests total
- MsqWriteService `insert_missing` flag (Fragile area #1 fix) — `MsqWriteService.save(insert_missing=True)` now injects new `<constant>` nodes into the first `<page>` element for any staged or base value whose name is absent from the source XML, instead of silently dropping it; iterates base constants then staged-only values with deduplication; constructs the right `name`/`units`/`rows`/`cols`/`digits` attributes from the `TuneValue`; default `insert_missing=False` preserves the historical byte-stable behaviour so 61 prior round-trip tests pass unchanged; 6 focused tests in `test_msq_write_insert_missing.py` covering default-off no-regression, table insertion, scalar insertion, staged-only values with no base entry, existing constant preservation, and idempotent re-save (no duplicate node after a second pass); CLAUDE.md fragile area #1 updated to reflect the available fix; 1728 tests total
- Wideband calibration service layer — new `WidebandCalibrationService` + `WidebandPreset(name, voltage_low, afr_at_voltage_low, voltage_high, afr_at_voltage_high, notes)` + `WidebandCalibrationResult(preset_name, afrs)` + `WidebandCalibrationPage.O2 = 2` enum in `wideband_calibration_service.py`; 5 published presets (Innovate LC-1/LC-2/LM-1/LM-2 7.35–22.39 AFR, AEM 30-0300/4110/X 10–20 AFR, 14Point7 Spartan 2, Tech Edge 2J9/WBo2 9–19 AFR, PLX SM-AFR/DM-6 10–20 AFR); linear voltage→AFR with active-band clamping (so a 0.5–4.5 V preset stays clamped at 10/20 AFR outside the band rather than extrapolating); `encode_payload()` produces the 64-byte big-endian int16 (AFR × 10) format Speeduino's `processCalibrationNew` page 2 expects; `build_serial_command()` produces the full 71-byte `'t'` packet ready for `SpeeduinoControllerClient.write_calibration_table()`; 12 focused tests in `test_wideband_calibration_service.py` covering preset catalog, lookup, generation, AEM and Innovate endpoint accuracy, monotonic behavior, payload encoding (length, big-endian int16, header bytes), zero-span error, and active-band clamping; closes the wideband calibration gap matrix row down to UI-only work; 1722 tests total
- Endianness consumer (Fragile area #3) — `EcuDefinition.is_little_endian()` and `byte_order()` are the canonical helpers for future byte-order-aware code paths; default little, case-insensitive, whitespace-tolerant, unknown values fall back to little; `byte_order()` returns the form Python's `int.from_bytes`/`to_bytes` accept directly; current Speeduino raw-protocol byte paths still hard-code little but the helpers give them one place to query when reworked; gap matrix row + CLAUDE.md fragile area #3 updated; 6 focused tests in `test_endianness_consumer.py`; 1710 tests total
- Gap matrix doc fixes — `ControllerCommands dispatch` row promoted from **Missing** → **Implemented** (was stale; `HardwareTestPanel` + `send_controller_command()` + 29 tests landed in an earlier slice); `Tune-backed table materialization` row updated to reflect that `test_blank_table_materialization.py` already covers the absent-table, generator-staged round-trip, AND the documented "tables absent from source XML are dropped" limitation
- Phase 7 workspace UI surfacing — `VeAnalyzeReviewService.build()` now accepts optional `smoothed_layer: SmoothedProposalLayer | None` and `diagnostics: RootCauseDiagnosticReport | None` kwargs; counts `clamp_applied` and `boost_penalty_applied > 0` cells in a single pass over `VeAnalysisSummary.cell_corrections`; emits four additive detail-text sections only when their signal fires (clamp transparency, boost penalty, smoothed layer summary, root-cause diagnostics block); new `clamp_count`, `boost_penalty_count`, `smoothed_summary_text`, and `diagnostic_lines` fields on `VeAnalyzeReviewSnapshot` with default-off values so all 23 prior review/coverage tests pass unchanged; 6 focused tests in `test_ve_analyze_review_phase7_surfacing.py` covering default-off no-regression, clamp count line, boost penalty count line, smoothed layer surfacing, diagnostic lines (with severity prefix), and empty-diagnostics no-emit; closes Phase 7; 1704 tests total
- Phase 5 final polish (table cell density) — `compute_table_cell_width(viewport_width, columns)` extracted from `TuningWorkspace._fit_table_column_widths` to a pure module-level helper so it can be unit-tested without instantiating Qt widgets; clamp range raised from `[44, 56]` → `[44, 80]` so 1440p+ workspaces no longer waste horizontal space on ≤12-column tables; old ceiling of 56 documented in the test suite as the regression baseline; 7 focused tests in `test_table_cell_density.py` covering zero columns, narrow viewport floor, wide viewport ceiling, balanced math, monotonic behavior in viewport width, monotonic behavior in column count, and the explicit polish verification (1500 px / 12 cols → > 56 px); Phase 5 promoted to Complete; 1698 tests total
