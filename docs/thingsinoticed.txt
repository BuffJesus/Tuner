cannot change logging channels, even if new profile created  [FIXED 2026-04-16: LOGGING tab had a read-only info card listing the profile's channels; replaced with a scrollable QCheckBox list + All/None batch toggles. Toggling a row mutates profile->channels[i].enabled, mirrors into the owning all_profiles slot, refreshes profile_channel_names + the channel-count label, and persists via save_profiles(). Profile-switch handler now also rebuilds the checkbox list so the new profile's enabled state lands on screen.]
logging page doesn't take up available vertical room when maximized (would be nice if logging graph took up more vertical space, and logging profile would take up less space)  [FIXED 2026-04-16: timeline widget now claims vertical stretch (addWidget(timeline, 1)) and the trailing addStretch(1) was removed — timeline grows with the window.]
triggers page doesn't take up available vertical room either  [FIXED 2026-04-16: results_widget now claims vertical stretch (addWidget(results_widget, 1)); trailing addStretch(1) removed.]
Setup page is not displaying proper values etc, needs work (incorrect VE Table, assumptions, AFR Target, assumptions, spark advance, pretty much everything)  [FIXED 2026-04-16: the preview was hardcoded to SINGLE_TURBO + 280° race cam + race_ported head + short_runner_plenum manifold + intercooler=true + CR=10.5 for cranking, regardless of tune contents. Stock NA engines (e.g. Ford 300) saw wildly wrong previews. Now reads boostEnabled scalar from the tune to pick NA vs SINGLE_TURBO topology, uses stock-friendly 250° cam, baseline head/manifold (empty strings = no bonuses/penalties), intercooler flag follows boost, AFR boost_target_kpa uses atmospheric (100 kPa) when NA. CR for cranking now reads from the tune. Every preview title now includes the actual detected context ("NA" / "Single Turbo, 200 kPa, intercooled") + data source (loaded tune / no tune loaded) so the operator can verify the inputs at a glance.]
live page could use some work (could take up more vertical space, as it doesn't take up available vertical space)  [FIXED 2026-04-16: dash_container now claims vertical stretch; trailing addStretch(1) removed. Gauges grow with window.]
Gauges could take up more vertical room  [FIXED 2026-04-16: same fix — dash_container stretches, so each QGridLayout gauge cell absorbs extra height.]
Full screen dashboard does not take up available screen space  [FIXED 2026-04-16: fullscreen grid layout now uses addLayout(grid, 1) so the gauge grid fills the screen instead of hugging the top.]
Some kind of indicator that writing to ram and burning were successful would be nice  [FIXED 2026-04-16: Write to RAM + Burn to Flash now show status-bar toast on success ("✅ Wrote N parameter(s)" / "🔥 Burned N page(s)") and a modal warning with first error message on partial/full failure that keeps the review dialog open for retry. Offline paths emit an informational toast noting staged-only.]
Injector timing page has all values set to 355, not sure if correct  [CLARIFIED 2026-04-16: 355° IS the Speeduino firmware's conservative default for every injAng channel (INI: `defaultValue = injAng, 355 355 355 355`) — not a bug. To make this visible instead of a "is this right?" moment, added a specific page_context_hint for "injector close / injector timing / injection angle" titles: "355° is the firmware default — a safe, conservative starting point. Most engines run well at the default until VE tuning is stable; revisit only if spray-pattern timing against the intake valve becomes limiting." Match ordered BEFORE the broader "injector" fallback so it isn't swallowed by the hardware-characteristics hint.]
Little bug I just noticed on injector timing page, the rpm values are listed in degrees celcius  [FIXED 2026-04-16: render_1d_curve hardcoded °C for x-axis bins; now uses curve.x_label]
Same bug in voltage correction, voltage listed in degrees celcius  [FIXED 2026-04-16: same render_1d_curve bug — one shared code path]
Might be bug across multiple pages
Definitely a bug in every curve page I checked  [FIXED 2026-04-16: see above]
Guided hardware setup would be nice (IAC, fan, etc)  [PARTIAL 2026-04-16: three SETUP-tab cards added after Cranking Assumptions. (a) "Hardware Outputs" — warning-accent — IAC Algorithm / Fan Mode / Fan On Temp / Fan Hysteresis. (b) "Flex Fuel Sensor" — purple-accent — Enable / Low Freq (E0) / High Freq (E100) + GM / Continental 50-150 Hz one-click preset. (c) "Safety & Protection" — danger-accent — Knock Detection Mode (Off/Digital/Analog), Knock Threshold (V), AFR Protection (Off/Fixed/Table), AFR Deviation (AFR), Cut Delay (s), Min MAP to arm (kPa), Min RPM to arm, Min TPS to arm (%), Reactivation TPS (%) — full 9-field AFR-protection parity with the deleted Python wizard. Every control reads current from edit_svc and stages on change; shared status chip per card. (d) "Advanced Turbo Parameters" — primary-accent — only shown when boostEnabled=On. 4 inputs: Surge Flow (lb/min), Choke Flow (lb/min), Max Pressure Ratio, Turbine A/R. Each persists to QSettings (setup/turbo/*) on edit. The Compressor-Map card below now reads these values instead of hardcoded GT30-class defaults, so the surge/choke/off-map risk classification reflects the operator's actual turbo datasheet. Status chip tells operators to revisit the tab to see the map recompute. (e) "Engine Advanced" — ok-accent — 3 dropdowns: Stroke Type (Four-stroke/Two-stroke), MAP Sampling (Instantaneous/Cycle Average/Cycle Minimum/Event Average), Fuel Trim Master (No/Yes). (f) "TPS Calibration" — primary-accent — TPS Min (ADC) + TPS Max (ADC) manual entries, each paired with a "Capture closed" / "Capture WOT" button that reads the live tpsADC channel from ecu_conn->runtime and stamps it into the field + stages the value. Capture buttons disable with a tooltip when not connected; manual entry remains available for operators who probe with a multimeter. **Full parity with the deleted Python wizard's hardware-setup surfaces.**]
Only see sequential fuel trim (1-4), even though the engine is a 6 cylinder in the project  [FIXED 2026-04-16: firmware-side constraint (Speeduino only ships 4 fuelTrim tables), so the desktop can't add channels — but the silent "why only 4?" confusion is fixable. Fuel trim table pages now append a contextual ⚠ note to the context header when nCylinders > 4: "Firmware only exposes 4 fuel-trim channels. On a %d-cylinder engine each trim table maps to injector output 1–4 per the Injection Layout setting — NOT to a specific cylinder. Expect shared trims where multiple cylinders share an output." Title match: case-insensitive "fuel trim" / "fueltrim" — catches every humanized variant. No-op on ≤4-cyl engines.]
Project is located here for reference D:\Documents\Tuner\Projects\TestProject
Save tune does not save the current tune in the active project directory it seems  [FIXED 2026-04-16: (1) Save Tune dialog fallback now defaults to project dir + <basename>.tuner. (2) New Project copies an external/bundled source tune into the project dir as <name>.tuner so Ctrl+S lands there instead of overwriting the source file.]
Filter toggles for the tree would be nice (icons that match the entries, as well as text. table icon with table text for example)  [FIXED 2026-04-16 — TUNE tab, left panel above the search box: added a "Filter:" row with three checkable buttons "Scalars", "Tables", "Curves". Each button carries the same make_tree_leaf_icon glyph the tree leaves use ('s' pill / 't' grid / 'c' sweep) so the visual grammar matches. Icons are 14×14 to fit the chip. enabled_kinds shared_ptr<set<char>> drives rebuild_tree: a leaf is hidden when its kind is NOT in the set. Kind filter composes with the search needle — rebuild_tree's narrowing path now triggers when the search needle is non-empty OR kind_filter is active (fewer than 3 kinds enabled). Unchecking a button hides all leaves of that kind and collapses empty groups/subgroups accordingly; re-checking restores them. TreeLeafInfo gained a `char kind` field so the tree build pass records kind once and the filter lambda reads it without re-parsing type_tag strings.]
Updating firmware signature, since we have ventured far from the source code by this point
Updating README.md for the firmware, tuner, and airbear to better tie the ecosystem together
Including everything in a single unified GitHub maybe? Tuner, firmware, airbear etc in a centralized location for users
Release zip so users can easily use everything immediately
Increasing guidance in the application for users who are not familiar with tuning (alot of overwhelming information available)  [PARTIAL 2026-04-16: TUNE tab empty-state welcome card extended with a "New to tuning? File → New Project runs the Engine Setup Wizard to generate a complete starter tune." breadcrumb (applied both to the initial welcome and to the post-page-selection fallback). Broader beginner-mode work still TBD.]
Pressing save project does not default to the folder location specified in the initial setup page for a new project  [FIXED 2026-04-16: New Project now persists the on-disk project dir to QSettings (projects/current/dir). Save Project falls through msq_path parent → project dir → last_tunerproj_dir → ~/Documents so the dialog starts where the operator expects.]
Pressing save project does not default to the last folder the project was saved in  [FIXED 2026-04-16: Save Project now writes QSettings projects/last_tunerproj_dir on every successful save; the next Save Project uses it as fallback when the project has no msq_path or project dir.]
Some naming inconsistencies in the application (VE Table and Second Fuel Table for instance)  [FIXED 2026-04-16: humanize map rule unified — primary tables have no suffix, paired secondaries add " 2". "Second Fuel Table" → "VE Table 2", "Second Spark Table" → "Spark Advance Table 2". AFR table pair already followed this rule ("AFR Target Table" / "AFR Target Table 2"). Now the three dual-table pairs share the same grammar so tree scanning reads consistently.]
Would be nice if curves displayed as an editable curve (like cranking enrichment curve for example)  [FIXED 2026-04-16: new EditableCurveChartWidget replaces the old horizontal-bar rendering on every curve page. Draws a line chart with filled area + vertex dots + horizontal grid + Y-axis labels + first/middle/last X-axis labels. Click any vertex and drag vertically to change that bin's Y value — commits the stage_list_cell + refreshes the matching QLineEdit text in the table below + updates the workspace review count / page chip / tree state indicators in the same paths the table edit uses. Table edits still work and push back into the chart via set_y_value so both surfaces stay byte-identical. Y-axis range pulls from CurveDefinition.y_axis when set; otherwise derived from data with ±10% margin. Defined at file scope before CellClickFilter so build_tune_tab can construct the widget; no Q_OBJECT — std::function callbacks keep moc out of the loop.]
Curve controls like smoothing etc would be nice  [FIXED 2026-04-16: three transform buttons sit above every curve page's chart — "Smooth" (3-point moving average, endpoints held), "Interpolate" (linear ramp from first Y to last Y across all bins), "Revert" (restore original Y values captured at page-open time). Each button computes a new values vector then runs it through a shared apply_transform that stages only the indices that actually changed via stage_list_cell, updates live_y / chart / row_edits in lockstep, and fires the same review-chip / tree-indicator refresh the drag + table paths use. Store the apply lambda behind a shared_ptr<function> to keep per-button capture lists flat (hit a MinGW 15.2 internal compiler error otherwise).]
Auto detecting the ECU port would be nice (in project setup, connection page, flashing page, etc)  [FIXED 2026-04-16 — Connection dialog: added an "Auto" button next to the port refresh button. Iterates every enumerated COM port, opens a SerialTransport at 115200, calls SpeeduinoController::connect with single-baud probe ({115200}, 'Q', 0.5s settle). First port whose signature contains "speeduino" (case-insensitive) wins — auto-fills port combo + forces baud to 115200. Per-port progress shown on a dedicated status line below the port row. Failed ports (busy / wrong baud / no response) fall through silently. Non-default bauds still require the manual baud combo as a fallback. Project setup + Flashing page auto-detect would need similar surfaces — not yet wired.]
Connection dialog: transport type + serial port + baud + TCP host + TCP port now persist across sessions via QSettings (session/last_transport, session/last_serial_port, session/last_serial_baud, session/last_tcp_host, session/last_tcp_port). Restored on dialog open; written only after a connect actually succeeds so a failed attempt doesn't overwrite a previously-good config.  [POLISH 2026-04-16]
In Tunerstudio, many changes require power cycling the ecu for them to take effect. This is not the case in the application.  [FIXED 2026-04-16: INI's [ConstantsExtensions] requiresPowerCycle list (already parsed in sub-slice 117) is now surfaced three ways: (1) a small ⟳ chip next to each affected scalar's label with tooltip "Changes take effect only after the ECU is power-cycled. Burn, then disconnect and reconnect power to the ECU.", (2) the tooltip on the label itself appends "⚠ Requires ECU power cycle after burn", (3) the Write-to-RAM success toast counts staged power-cycle params and shows "⟳ N change(s) require a power cycle after burn" with a longer 10s timeout when present. AUTO-REBOOT: after a successful Burn to Flash that included any power-cycle params, the app now prompts with a QMessageBox::question. On STM32 firmware (detected by the presence of `cmdstm32reboot` in the parsed [ControllerCommands]) it offers "Attempt automatic reboot now?" — Yes sends the E\x32\x00 command via controller->fetch_raw (ECU resets mid-exchange; the timeout exception is the success signal), then closes the connection and prompts the operator to reconnect. On firmware without that command (AVR/Teensy production builds) it falls back to "Disconnect and reconnect ECU power, then click OK to reconnect" and closes the connection on confirm. Shared pending_reboot_count carries the Write-time pc count across to the Burn handler via a shared_ptr<int> at build_tune_tab scope.]